<h1><%= @race.name %></h1>

<div>
  <p>일시: <%= l @race.event_date, format: :long %></p>
  <p>장소: <%= @race.location %></p>
  <p>신청 마감: <%= l @race.registration_deadline, format: :long %> <%= link_to "수정", edit_admin_race_path %></p>
</div>

<h2>코스 목록</h2>

<table>
  <thead>
    <tr>
      <th>코스명</th>
      <th>정원</th>
      <th>신청 수</th>
      <th>잔여</th>
      <th>참가비</th>
      <th>출발 시간</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course.name %></td>
        <td><%= course.capacity %></td>
        <td><%= course.applied_count %></td>
        <td><%= course.remaining_slots %></td>
        <td><%= number_with_delimiter(course.fee) %>원</td>
        <td><%= course.start_time.strftime("%H:%M") %></td>
        <td><%= link_to "수정", edit_admin_course_path(course) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "신청자 목록", admin_registrations_path %>

<%= button_to "로그아웃", admin_logout_path, method: :delete %>
